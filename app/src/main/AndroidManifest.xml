<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.mobpro.hslu.itengebs.helloeve">

    <!-- android:roundIcon="@mipmap/ic_launcher_round" -->
    <application
        android:name="com.orm.SugarApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon">
        <meta-data
            android:name="DATABASE"
            android:value="helloEve.db" />
        <meta-data
            android:name="VERSION"
            android:value="1" />
        <meta-data
            android:name="QUERY_LOG"
            android:value="true" />
        <meta-data
            android:name="DOMAIN_PACKAGE_NAME"
            android:value="com.mobpro.hslu.itengebs.helloeve.model" />

        <activity
            android:name=".LaunchActivity_"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity_" />
        <activity
            android:name=".RegisterActivity_"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".HistoryActivity_" />
        <activity android:name=".MessagePrefActivity" />
    </application>

</manifest>